export enum StorageKeys {
    LISTS = "lists",
}

export enum IconName {
    Bars = "bars",
    CompletedCheckMarkCircle = "completed-checkmark-circle",
    Home = "home",
    Star = "star",
    Stack = "stack",
    Sun = "sun",
}
